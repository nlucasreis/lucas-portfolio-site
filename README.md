# TheYuriG — static clone

Uma recriação estática da landing page de referência em HTML, CSS e JavaScript puros.

Para visualizar localmente:

```powershell
python -m http.server 4173
```

Depois abra `http://localhost:4173` no navegador.
